export const labels=['Frequency (MHz)','Distance (km)','TX power (dBm)','TX antenna gain (dBi)','RX antenna gain (dBi)','Amplifier gain (dB)','TX cable loss (dB)','RX cable loss (dB)','Receiver sensitivity (dBm)','Required margin (dB)'];
export function number(x){const s=String(x).trim().replaceAll(',','.');return /^[+-]?(?:\d+\.?\d*|\.\d+)(?:e[+-]?\d+)?$/i.test(s)&&Number.isFinite(Number(s))?Number(s):NaN;}
export function budget(v){if(v.length!==10)throw Error('Ten link parameters are required');v.forEach((x,i)=>{if(!Number.isFinite(x)||(i<2&&x<=0)||([6,7,9].includes(i)&&x<0))throw Error(`Please check ${labels[i]}`);});const [f,d,p,t,r,a,tl,rl,s,m]=v;if(d<1e-140||d>1e140)throw Error('Distance is outside the analysis range');const fspl=20*Math.log10(d)+20*Math.log10(f)+32.44,received=p-tl+t+r-fspl,output=received-rl+a,margin=output-s;const stages=[['Transmitter output','Initial TX power',p],['TX antenna input',`TX cable loss −${tl} dB`,p-tl],['EIRP',`TX antenna gain ${t} dB`,p-tl+t],['After free-space loss',`FSPL −${fspl} dB`,p-tl+t-fspl],['RX antenna output',`RX antenna gain ${r} dB`,received],['RX amplifier input',`RX cable loss −${rl} dB`,received-rl],['Receiver input',`Amplifier gain ${a} dB`,output]];if(![fspl,received,output,margin,...stages.map(x=>x[2]),s+m].every(Number.isFinite))throw Error('Values are outside the calculation range');const maxDistance=10**(Math.log10(d)+(margin-m)/20);return {fspl,received,output,margin,stages,maxDistance:Number.isFinite(maxDistance)&&maxDistance>0?maxDistance:null,meets:margin>=m,status:margin<0?'Below receiver sensitivity':margin<m?'Margin target not met':'Margin target met'};}
export function powerAt(v,d){return budget(v).output-20*(Math.log10(d)-Math.log10(v[1]));}
export function noise(v,bw,nf,target){if(![bw,nf,target].every(Number.isFinite)||bw<=0||nf<0)return null;const floor=-173.975+10*Math.log10(bw)+nf,snr=budget(v).received-floor,margin=snr-target;return [floor,snr,margin].every(Number.isFinite)?{floor,snr,margin}:null;}
export function convertPower(x,toWatts){if(!Number.isFinite(x)||(!toWatts&&x<=0))return null;const y=toWatts?10**((x-30)/10):10*Math.log10(x)+30;return Number.isFinite(y)&&(!toWatts||y>0)?y:null;}
export function doppler(f,v){if(![f,v].every(Number.isFinite)||f<=0||Math.abs(v)>2997924.58)return null;const shift=-f*(v/299792458),received=f+shift;return Number.isFinite(shift)&&Number.isFinite(received)&&received>0?{shift,received}:null;}
export function validPlan(p){try{return !!p&&typeof p.id==='string'&&typeof p.name==='string'&&typeof p.notes==='string'&&typeof p.date==='string'&&Number.isFinite(Date.parse(p.date))&&!!budget(p.values);}catch{return false;}}
